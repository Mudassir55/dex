{
  "installCommand": "yarn install --frozen-lockfile --ignore-optional",
  "buildCommand": "yarn build",
  "outputDirectory": "build",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
