/* eslint-env node */

// Keep the default CRACO config filename as the single entry point used by
// local development and deployment platforms such as Vercel.
module.exports = require('./craco.config.cjs')
